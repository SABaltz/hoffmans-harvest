import {createTheme} from "@mui/material";

export const plantTheme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#196904',
        },
        secondary: {
            main: '#7a7a7a'
        }

    },
    typography: {
        fontFamily: 'poppins',
    },
})
